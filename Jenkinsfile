pipeline {
  enviroment {
    HOME = '/var/lib/jenkins'
  }
  stages {
    stage('Build') {
      steps {
        sh 'npm run start-dev'
      }
    }
    stage('Deploy') {
      steps {
        sh
      }
    }
  }

}
